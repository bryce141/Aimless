// Template. Copy to Aimless/Config.swift and paste your key.
// Aimless/Config.swift is gitignored; this file is not, so keep it keyless.
//
// Sits at the repo root rather than inside Aimless/ so Xcode's synchronized
// folder doesn't compile it as a duplicate of the real Config.

import Foundation

enum Config {
    static let orsAPIKey = "PASTE_YOUR_OPENROUTESERVICE_KEY_HERE"
}
